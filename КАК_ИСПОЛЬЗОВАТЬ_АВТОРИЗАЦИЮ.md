# 🔐 КАК ИСПОЛЬЗОВАТЬ АВТОРИЗАЦИЮ В SWAGGER

## ❌ ПРОБЛЕМА:

Ошибка: `"invalid authorization header format"`

**Причина:** В Swagger нужно указывать токен в формате `Bearer <token>`, а не просто токен.

---

## ✅ ПРАВИЛЬНОЕ ИСПОЛЬЗОВАНИЕ:

### Шаг 1: Получите JWT токен

1. В Swagger найдите `POST /auth/login`
2. Выполните запрос:

```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

3. Скопируйте токен из ответа (поле `token`)

### Шаг 2: Авторизуйтесь в Swagger

1. Нажмите кнопку **"Authorize"** вверху справа (замочек 🔒)
2. В поле **"Value"** вставьте: `Bearer <ваш_токен>`

**Важно:** Обязательно добавьте слово `Bearer` и пробел перед токеном!

**Пример:**
```
Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjo2LCJlbWFpbCI6InVzZXJAZXhhbXBsZS5jb20iLCJyb2xlIjoidXNlciIsImV4cCI6MTc2NTIyNTI2NywiaWF0IjoxNzY1MTM4ODY3fQ.o-DaFNG2JmxWMP2xUNrk3CufJ1eTmbbv5fEq3ThMvW0
```

3. Нажмите **"Authorize"**
4. Нажмите **"Close"**

### Шаг 3: Выполните запрос

Теперь все запросы, требующие авторизации, будут автоматически использовать этот токен.

---

## 📝 ПРИМЕР:

**Неправильно:**
```
Authorization: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Правильно:**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🔍 ПРОВЕРКА:

После авторизации:

1. Кнопка "Authorize" должна стать зеленой (🔒)
2. При выполнении запроса `GET /api/requests` заголовок должен быть:
   ```
   Authorization: Bearer <токен>
   ```
3. Запрос должен вернуть статус **200 OK** (если есть заявки)

---

## ⚠️ ВАЖНО:

- **Всегда** используйте формат `Bearer <token>`
- Не добавляйте кавычки вокруг токена
- После авторизации токен сохраняется для всех запросов
- Для выхода нажмите "Authorize" и "Logout"

---

## 🎯 БЫСТРАЯ ИНСТРУКЦИЯ:

1. `POST /auth/login` → получите токен
2. Нажмите "Authorize" (замочек)
3. Вставьте: `Bearer <ваш_токен>`
4. Нажмите "Authorize" → "Close"
5. Выполните `GET /api/requests` → должно работать!

---

## 📝 ПРИМЕЧАНИЕ О ДАТАХ:

Также заметил, что вы используете дату `2026-02-30`, но в феврале 2026 года только 28 дней. Используйте правильные даты:

- `2026-01-01` до `2026-02-28` ✅
- `2026-01-01` до `2026-02-30` ❌ (неверная дата)


