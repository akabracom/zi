# 🔄 ИНСТРУКЦИЯ ПО ПОЛНОМУ ПЕРЕЗАПУСКУ

## ❌ ПРОБЛЕМА: Изменения не видны после перезапуска

**Причина:** Браузер кэширует старые файлы JavaScript.

---

## ✅ РЕШЕНИЕ (пошагово):

### Шаг 1: Остановить dev server

1. Найдите терминал, где запущен `npm run dev`
2. Нажмите **Ctrl+C** для остановки

### Шаг 2: Убедиться, что файл .env создан

```cmd
cd C:\spa-frontend\spa-deposits
type .env
```

**Должно показать:** `VITE_USE_MOCK=true`

**Если файла нет:**
```cmd
echo VITE_USE_MOCK=true > .env
```

### Шаг 3: Остановить все процессы Node.js

```cmd
taskkill /F /IM node.exe
```

### Шаг 4: Запустить dev server заново

```cmd
cd C:\spa-frontend\spa-deposits
npm run dev
```

### Шаг 5: ОЧИСТИТЬ КЭШ БРАУЗЕРА (ВАЖНО!)

**Способ 1: Жесткая перезагрузка**
- Нажмите **Ctrl+Shift+R** (Windows/Linux)
- Или **Ctrl+F5**

**Способ 2: Очистить кэш вручную**
1. Откройте DevTools (F12)
2. Правой кнопкой на кнопку обновления
3. Выберите "Очистить кэш и жесткая перезагрузка"

**Способ 3: Открыть в режиме инкогнито**
- Нажмите **Ctrl+Shift+N** (Chrome) или **Ctrl+Shift+P** (Firefox)
- Откройте: http://localhost:5173

---

## 🎯 ЧТО ДОЛЖНО БЫТЬ ВИДНО:

### После правильного перезапуска:

✅ **Ставки:**
- Январь: **10.5%** (не 5.5%)
- Февраль: **10.2%** (не 5.0%)
- Март: **10.0%** (не 5.8%)

✅ **Изображения:**
- В mock режиме: серые заглушки с названиями месяцев
- В реальном режиме: изображения из MinIO

---

## 🚀 БЫСТРЫЙ СПОСОБ:

Используйте скрипт:

```cmd
cd C:\spa-frontend\spa-deposits
СОЗДАТЬ_ENV_И_ПЕРЕЗАПУСТИТЬ.bat
```

Или:

```cmd
cd C:\spa-frontend\spa-deposits
ЗАПУСТИТЬ_MOCK.bat
```

---

## ⚠️ ВАЖНО:

После перезапуска dev server **ОБЯЗАТЕЛЬНО** сделайте жесткую перезагрузку в браузере:
- **Ctrl+Shift+R** или **Ctrl+F5**

Иначе браузер будет показывать старые закэшированные файлы!

---

**Готово! 🎉**


