# 📸 ЧЕКЛИСТ СКРИНШОТОВ ДЛЯ ЛАБЫ 5 (spa-deposits)

## ✅ ПОРЯДОК ДЕМОНСТРАЦИИ (строго по порядку!)

---

## 📋 ЧАСТЬ 1: MOCK РЕЖИМ (БЕЗ БЭКЕНДА)

### **1. Включение mock режима**

**Действия:**
1. Создайте `.env` файл: `VITE_USE_MOCK=true`
2. Запустите: `npm run dev`
3. Откройте: http://localhost:5173

**Скриншот:**
- ✅ Файл `.env` с содержимым `VITE_USE_MOCK=true`
- ✅ Терминал с запущенным `npm run dev`

---

### **2. Главная страница (ServicesPage) с mock**

**Действия:**
1. Откройте: http://localhost:5173
2. Покажите список месяцев

**Скриншот:**
- ✅ Страница "Банковские месяцы"
- ✅ Список: Январь, Февраль, Март
- ✅ Поле поиска
- ✅ Кнопка "Добавить в расчет"

---

### **3. Работа поиска с mock**

**Действия:**
1. Введите в поиск: `Январь`
2. Нажмите "Найти"

**Скриншот:**
- ✅ Поле поиска с текстом "Январь"
- ✅ Отфильтрованный список (только "Январь")
- ✅ Код `filterMockServices` в `mock.ts` (выделить функцию)

---

### **4. Страница черновика (DraftPage) с mock**

**Действия:**
1. Войдите: `/login` → `user@example.com` / `password123`
2. Перейдите: `/draft`

**Скриншот:**
- ✅ Страница "Расчет вклада"
- ✅ Таблица с месяцами
- ✅ Расчет прибыли
- ✅ Общая сумма и прибыль

---

### **5. Страница заявок (RequestsPage) с mock**

**Действия:**
1. Перейдите: `/requests`

**Скриншот:**
- ✅ Страница "Расчеты вкладов"
- ✅ Таблица с заявками
- ✅ Статусы: submitted, completed
- ✅ Кнопки для модератора

---

## 📋 ЧАСТЬ 2: С БЭКЕНДОМ

### **6. Выключение mock режима**

**Действия:**
1. Измените `.env`: `VITE_USE_MOCK=false`
2. Перезапустите: `npm run dev`
3. Запустите бэкенд: `cd SPA_Dep && docker-compose up -d`

**Скриншот:**
- ✅ Файл `.env` с `VITE_USE_MOCK=false`
- ✅ Терминал с перезапущенным dev server

---

### **7. Главная страница с бэкендом (Network)**

**Действия:**
1. Откройте: http://localhost:5173
2. DevTools (F12) → Network
3. Обновите страницу

**Скриншот:**
- ✅ Страница "Банковские месяцы"
- ✅ DevTools → Network
- ✅ Запрос: `GET http://localhost:5173/api/services`
- ✅ Headers: проксирование на `localhost:3001`
- ✅ Response: данные из БД

---

### **8. Поиск с бэкендом (Network)**

**Действия:**
1. Введите в поиск: `Январь`
2. Нажмите "Найти"
3. Покажите Network

**Скриншот:**
- ✅ Поле поиска с "Январь"
- ✅ Network: `GET /api/services?query=Январь`
- ✅ Проксирование на `localhost:3001`
- ✅ Response с отфильтрованными данными

---

## 📋 ЧАСТЬ 3: NETWORK И MINIO

### **9. Fetch запрос через proxy (детально)**

**Действия:**
1. DevTools → Network
2. Найдите запрос `GET /api/services`
3. Покажите вкладки: Headers, Preview, Response

**Скриншот:**
- ✅ **Headers:**
  - Request URL: `http://localhost:5173/api/services`
  - Request Method: `GET`
  - Status Code: `200 OK`
- ✅ **Response:** JSON с данными услуг
- ✅ **Preview:** Отформатированный JSON

---

### **10. Запрос изображения через proxy**

**Действия:**
1. Network → найдите `GET /images/january.png`
2. Покажите Headers и Preview

**Скриншот:**
- ✅ **Headers:**
  - Request URL: `http://localhost:5173/images/january.png`
  - Status Code: `200 OK`
  - Content-Type: `image/png`
- ✅ **Preview:** Само изображение
- ✅ Объяснить путь: `localhost:5173` → proxy → `localhost:3001` → MinIO

---

### **11. Код формирования URL изображений**

**Действия:**
1. Откройте `src/pages/ServicesPage.tsx`
2. Найдите строки 108-111

**Скриншот:**
- ✅ Код:
  ```tsx
  src={s.image_url.startsWith('http') 
    ? s.image_url 
    : `http://localhost:3001/images/${s.image_url.replace(/^(img\/|images\/|deposits\/img\/)/, '')}`}
  ```
- ✅ Выделить логику формирования URL
- ✅ Показать, что `image_url` из БД - это только имя файла

---

### **12. MinIO Console - список изображений**

**Действия:**
1. Откройте: http://localhost:9001
2. Войдите: `minioadmin` / `minioadmin123`
3. Bucket `deposits` → папка `img/`

**Скриншот:**
- ✅ MinIO Console
- ✅ Bucket `deposits`
- ✅ Папка `img/` с файлами:
  - `january.png`
  - `february.png`
  - `march.png`
- ✅ Превью изображений (если доступно)

---

## 📋 ЧАСТЬ 4: ИЗМЕНЕНИЯ В БД

### **13. Добавление месяца в БД (Adminer)**

**Действия:**
1. Adminer: http://localhost:8080
2. Выполните SQL:
   ```sql
   INSERT INTO services (name, description, price, image_url, is_active) 
   VALUES ('Апрель', 'Дней: 30 | Ставка: 6.0% | Сезон: Весна', 6.0, 'april.png', true);
   ```

**Скриншот:**
- ✅ Adminer открыт
- ✅ SQL запрос в редакторе
- ✅ Результат: "1 row affected"
- ✅ Таблица `services` с новой записью

---

### **14. Новый месяц во фронтенде**

**Действия:**
1. Вернитесь во фронтенд
2. Обновите страницу (F5)
3. DevTools → Network

**Скриншот:**
- ✅ Страница "Банковские месяцы"
- ✅ В списке появился "Апрель"
- ✅ Network: запрос `GET /api/services`
- ✅ Response содержит "Апрель"

---

### **15. Изменение данных в БД**

**Действия:**
1. В Adminer выполните:
   ```sql
   UPDATE services 
   SET price = 7.5 
   WHERE name = 'Январь';
   ```
2. Обновите фронтенд

**Скриншот:**
- ✅ Adminer: SQL запрос выполнен
- ✅ Фронтенд: цена "Январь" = 7.5%
- ✅ Network: новый запрос с обновленными данными

---

## 📋 ЧАСТЬ 5: ОБЪЯСНЕНИЕ КОДА

### **16. ServicesPage - структура и хуки**

**Скриншот:**
- ✅ Файл `src/pages/ServicesPage.tsx`
- ✅ Импорты (выделить)
- ✅ useState хуки (выделить)
- ✅ useAuth, useRole (выделить)

---

### **17. ServicesPage - функция load()**

**Скриншот:**
- ✅ Функция `load()` (строки 17-28)
- ✅ Выделить: `getServices(query)`
- ✅ Выделить: `setServices(data)`
- ✅ Выделить: обработку ошибок

---

### **18. ServicesPage - useEffect**

**Скриншот:**
- ✅ `useEffect(() => { void load() }, [])` (строки 44-48)
- ✅ Объяснить: выполняется один раз при монтировании

---

### **19. ServicesPage - handleSearch**

**Скриншот:**
- ✅ Функция `handleSearch` (строки 50-53)
- ✅ Форма с `onSubmit={handleSearch}`
- ✅ Input с `onChange={(e) => setQuery(e.target.value)}`

---

### **20. http.ts - функция getServices**

**Скриншот:**
- ✅ Файл `src/api/http.ts`
- ✅ Функция `getServices` (строки 85-97)
- ✅ Выделить: проверку `USE_MOCK`
- ✅ Выделить: формирование URL с query
- ✅ Выделить: вызов `apiFetch`

---

### **21. http.ts - функция apiFetch**

**Скриншот:**
- ✅ Функция `apiFetch` (строки 22-65)
- ✅ Выделить: формирование headers
- ✅ Выделить: добавление Authorization
- ✅ Выделить: вызов `fetch`
- ✅ Выделить: обработку ошибок

---

### **22. vite.config.ts - proxy конфигурация**

**Скриншот:**
- ✅ Файл `vite.config.ts`
- ✅ Секция `proxy` (строки 9-22)
- ✅ Выделить: `/api` → `localhost:3001`
- ✅ Выделить: `/images` → `localhost:3001`

---

### **23. DraftPage - расчет прибыли**

**Скриншот:**
- ✅ Файл `src/pages/DraftPage.tsx`
- ✅ Функция `extractDays` (строки 36-40)
- ✅ Функция `calculateProfit` (строки 43-45)
- ✅ `useMemo` с расчетом totals (строки 50-74)
- ✅ Формула прибыли (выделить)

---

### **24. RequestsPage - фильтрация по ролям**

**Скриншот:**
- ✅ Файл `src/pages/RequestsPage.tsx`
- ✅ `const role = useRole()` (строка 10)
- ✅ Условный рендеринг: `{role === 'moderator' && ...}` (строки 77-94)
- ✅ Кнопки "Завершить" и "Удалить"

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Перед демонстрацией:

- ✅ Mock режим работает
- ✅ Реальный режим работает
- ✅ Бэкенд запущен
- ✅ Proxy работает
- ✅ Изображения загружаются
- ✅ Все скриншоты готовы

---

**Всего скриншотов: 24**

**Удачи! 🚀**


