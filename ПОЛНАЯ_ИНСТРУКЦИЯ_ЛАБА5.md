# 🔬 ЛАБОРАТОРНАЯ РАБОТА 5 (spa-deposits) - ПОЛНАЯ ИНСТРУКЦИЯ

## 📋 СОДЕРЖАНИЕ

1. [Подготовка окружения](#1-подготовка-окружения)
2. [Часть 1: Mock режим (без бэкенда)](#2-часть-1-mock-режим-без-бэкенда)
3. [Часть 2: С бэкендом](#3-часть-2-с-бэкендом)
4. [Часть 3: Network и MinIO](#4-часть-3-network-и-minio)
5. [Часть 4: Изменения в БД](#5-часть-4-изменения-в-бд)
6. [Часть 5: Объяснение кода](#6-часть-5-объяснение-кода)

---

## 1. ПОДГОТОВКА ОКРУЖЕНИЯ

### 📍 Шаг 1: Проверка структуры проекта

Убедитесь, что у вас есть:
- ✅ Папка `spa-deposits` с фронтендом
- ✅ Папка `SPA_Dep` с бэкендом
- ✅ Node.js установлен (проверьте: `node --version`)
- ✅ npm установлен (проверьте: `npm --version`)

### 📍 Шаг 2: Установка зависимостей (если еще не установлены)

```cmd
cd C:\spa-frontend\spa-deposits
npm install
```

**Ожидаемый результат:** Зависимости установлены, папка `node_modules` создана.

---

## 2. ЧАСТЬ 1: MOCK РЕЖИМ (БЕЗ БЭКЕНДА)

### 📍 Шаг 1: Остановить бэкенд (опционально)

Для чистоты демонстрации можно остановить бэкенд:

```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose stop deposits-app
```

**Или оставить запущенным** - mock режим работает независимо.

---

### 📍 Шаг 2: Включить mock режим

**Вариант 1: Через скрипт (РЕКОМЕНДУЕТСЯ)**

```cmd
cd C:\spa-frontend\spa-deposits
ЗАПУСТИТЬ_MOCK.bat
```

**Вариант 2: Вручную**

1. Перейдите в папку проекта:
   ```cmd
   cd C:\spa-frontend\spa-deposits
   ```

2. Создайте файл `.env` в корне проекта:
   - Откройте блокнот
   - Введите: `VITE_USE_MOCK=true`
   - Сохраните как `.env` (без расширения, тип файла: "Все файлы")

3. Запустите dev server:
   ```cmd
   npm run dev
   ```

**Ожидаемый результат:**
```
  VITE v7.2.4  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

### 📍 Шаг 3: Открыть приложение

1. Откройте браузер
2. Перейдите: http://localhost:5173
3. Должна открыться страница "Банковские месяцы"

---

### 📸 СКРИНШОТ 1: Главная страница (ServicesPage) с mock данными

**Что сделать:**

1. Убедитесь, что страница открыта: http://localhost:5173
2. Должна быть видна страница "Банковские месяцы"
3. В списке должны быть: Январь, Февраль, Март

**Что показать на скриншоте:**
- ✅ Заголовок "Банковские месяцы"
- ✅ Подзаголовок с описанием
- ✅ Поле поиска
- ✅ Список карточек с месяцами (3 штуки)
- ✅ На каждой карточке:
  - Изображение месяца
  - Название (Январь, Февраль, Март)
  - Описание
  - Ставка в процентах
  - Кнопка "Добавить в расчет" (если авторизованы)

**Важно:** В консоли браузера (F12 → Console) не должно быть ошибок о недоступности API.

---

### 📸 СКРИНШОТ 2: Работа поиска с mock данными

**Что сделать:**

1. В поле поиска введите: `Январь`
2. Нажмите кнопку "Найти"
3. Должен остаться только "Январь" в списке

**Что показать на скриншоте:**
- ✅ Поле поиска с текстом "Январь"
- ✅ Кнопка "Найти" нажата
- ✅ Отфильтрованный список (только одна карточка "Январь")
- ✅ Остальные месяцы скрыты

**Дополнительно:** Покажите код фильтрации в `src/api/mock.ts`:

```typescript
export function filterMockServices(query: string): Service[] {
  if (!query) return mockServices
  const lowerQuery = query.toLowerCase()
  return mockServices.filter(
    (service) =>
      service.name.toLowerCase().includes(lowerQuery) ||
      service.description?.toLowerCase().includes(lowerQuery),
  )
}
```

**Объяснение:**
- Фильтрация происходит на клиенте (в браузере)
- Не делается запрос к серверу
- Используются данные из `mockServices` в `mock.ts`

---

### 📸 СКРИНШОТ 3: Страница черновика (DraftPage) с mock данными

**Что сделать:**

1. **Войдите в систему:**
   - Нажмите на ссылку "Войти" или перейдите: http://localhost:5173/login
   - Введите данные:
     - Email: `user@example.com`
     - Password: `password123`
   - Нажмите "Войти"
   - Должен произойти редирект на главную страницу

2. **Перейдите на страницу черновика:**
   - Нажмите на иконку корзины вверху справа
   - Или перейдите: http://localhost:5173/draft

3. **Покажите страницу расчета**

**Что показать на скриншоте:**
- ✅ Заголовок "Расчет вклада"
- ✅ Информация о расчете:
  - ID расчета (например, #1)
  - Статус: `draft`
- ✅ Таблица с месяцами:
  - Колонки: Месяц, Ставка (%), Количество, Прибыль
  - Данные из mock (например, Январь, количество: 2)
- ✅ Итоги внизу:
  - Общая сумма вкладов
  - Общая прибыль
  - Средняя ставка
- ✅ Кнопка "Отправить расчет"

**Важно:** Данные берутся из `mockDraft` в `src/api/mock.ts`.

---

### 📸 СКРИНШОТ 4: Страница заявок (RequestsPage) с mock данными

**Что сделать:**

1. Перейдите на страницу заявок:
   - В меню нажмите "Расчеты" или перейдите: http://localhost:5173/requests

2. Покажите список заявок

**Что показать на скриншоте:**
- ✅ Заголовок "Расчеты вкладов"
- ✅ Таблица с заявками:
  - Колонки: ID, Статус, Дата создания, Количество месяцев
  - Данные из mock (например, 2 заявки)
  - Статусы: `submitted`, `completed`
- ✅ Кнопки "Завершить" и "Удалить" (если роль модератор)

**Важно:** Данные берутся из `mockRequests` в `src/api/mock.ts`.

---

## 3. ЧАСТЬ 2: С БЭКЕНДОМ

### 📍 Шаг 1: Выключить mock режим

**Вариант 1: Через скрипт (РЕКОМЕНДУЕТСЯ)**

```cmd
cd C:\spa-frontend\spa-deposits
ЗАПУСТИТЬ_С_БЭКЕНДОМ.bat
```

**Вариант 2: Вручную**

1. Откройте файл `.env` в папке `spa-deposits`
2. Измените на: `VITE_USE_MOCK=false`
3. Или удалите файл `.env` полностью

4. Перезапустите dev server:
   ```cmd
   # Остановите текущий (Ctrl+C в терминале где запущен npm run dev)
   npm run dev
   ```

---

### 📍 Шаг 2: Запустить бэкенд

```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose up -d deposits-app
```

**Проверка:**
```cmd
docker-compose ps deposits-app
```

Должен быть статус `Up`.

**Подождите 10-15 секунд** для полной инициализации.

---

### 📍 Шаг 3: Проверить работу бэкенда

1. Откройте Swagger: http://localhost:3001/swagger/index.html
2. Выполните запрос `GET /api/services`
3. Должен вернуться список услуг из БД

---

### 📸 СКРИНШОТ 5: Главная страница с бэкендом (Network)

**Что сделать:**

1. Откройте браузер: http://localhost:5173
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Network**
4. Обновите страницу (F5)
5. Найдите запрос `GET /api/services`

**Что показать на скриншоте:**

**Вкладка Network:**
- ✅ Список запросов
- ✅ Запрос `GET /api/services` выделен
- ✅ Статус: `200 OK`
- ✅ Тип: `fetch` или `xhr`

**Вкладка Headers:**
- ✅ **General:**
  - Request URL: `http://localhost:5173/api/services`
  - Request Method: `GET`
  - Status Code: `200 OK`
- ✅ **Response Headers:**
  - `content-type: application/json`
  - `content-length: <размер>`

**Вкладка Response:**
- ✅ JSON массив с услугами из БД
- ✅ Данные должны отличаться от mock (если в БД другие данные)

**Объяснение:**
- Запрос идет на `localhost:5173` (Vite dev server)
- Vite proxy перенаправляет на `localhost:3001` (бэкенд)
- Бэкенд возвращает данные из PostgreSQL
- Данные отображаются на странице

---

### 📸 СКРИНШОТ 6: Работа поиска с бэкендом (Network)

**Что сделать:**

1. В поле поиска введите: `Январь`
2. Нажмите "Найти"
3. В Network найдите новый запрос

**Что показать на скриншоте:**

**Вкладка Network:**
- ✅ Новый запрос: `GET /api/services?query=Январь`
- ✅ Статус: `200 OK`

**Вкладка Headers:**
- ✅ **Request URL:** `http://localhost:5173/api/services?query=Январь`
- ✅ **Query String Parameters:**
  - `query: Январь`

**Вкладка Response:**
- ✅ JSON массив с отфильтрованными данными
- ✅ Только услуги, содержащие "Январь" в названии или описании

**Объяснение:**
- Фильтрация происходит на бэкенде
- Бэкенд выполняет SQL запрос с `WHERE name LIKE '%Январь%'`
- Возвращает только подходящие записи

---

## 4. ЧАСТЬ 3: NETWORK И MINIO

### 📸 СКРИНШОТ 7: Fetch запрос через proxy (детально)

**Что сделать:**

1. DevTools (F12) → вкладка **Network**
2. Обновите страницу (F5)
3. Найдите запрос `GET /api/services`
4. Откройте вкладки: **Headers**, **Preview**, **Response**

**Что показать на скриншоте:**

**Вкладка Headers → General:**
- ✅ Request URL: `http://localhost:5173/api/services`
- ✅ Request Method: `GET`
- ✅ Status Code: `200 OK`
- ✅ Remote Address: `127.0.0.1:5173`

**Вкладка Headers → Request Headers:**
- ✅ `Accept: application/json`
- ✅ `Origin: http://localhost:5173`
- ✅ `Referer: http://localhost:5173/`

**Вкладка Headers → Response Headers:**
- ✅ `content-type: application/json; charset=utf-8`
- ✅ `content-length: <размер>`

**Вкладка Preview:**
- ✅ Отформатированный JSON
- ✅ Массив услуг с полями: id, name, description, price, image_url

**Вкладка Response:**
- ✅ Сырой JSON текст

**Объяснение:**
- Запрос идет на `localhost:5173` (Vite)
- Vite proxy перенаправляет на `localhost:3001` (бэкенд)
- Бэкенд обрабатывает запрос → возвращает JSON
- Vite возвращает ответ клиенту

---

### 📸 СКРИНШОТ 8: Запрос изображения через proxy

**Что сделать:**

1. DevTools (F12) → вкладка **Network**
2. Обновите страницу (F5)
3. Найдите запрос типа `GET /images/january.png` (или другое изображение)
4. Откройте вкладки: **Headers**, **Preview**

**Что показать на скриншоте:**

**Вкладка Headers → General:**
- ✅ Request URL: `http://localhost:5173/images/january.png`
- ✅ Request Method: `GET`
- ✅ Status Code: `200 OK`
- ✅ Type: `png` или `image/png`

**Вкладка Headers → Response Headers:**
- ✅ `content-type: image/png`
- ✅ `content-length: <размер в байтах>`
- ✅ `cache-control: public, max-age=31536000`

**Вкладка Preview:**
- ✅ Само изображение (превью)
- ✅ Видно картинку месяца

**Объяснение:**
- Запрос: `localhost:5173/images/january.png`
- Vite proxy → `localhost:3001/images/january.png`
- Бэкенд получает из MinIO: `img/january.png`
- MinIO возвращает файл → бэкенд → Vite → браузер

---

### 📸 СКРИНШОТ 9: Код формирования URL изображений

**Что сделать:**

1. Откройте файл `src/pages/ServicesPage.tsx` в редакторе
2. Найдите строки 108-111 (где формируется src для img)

**Что показать на скриншоте:**

```tsx
{s.image_url && (
  <img
    src={s.image_url.startsWith('http') 
      ? s.image_url 
      : `http://localhost:3001/images/${s.image_url.replace(/^(img\/|images\/|deposits\/img\/)/, '')}`}
    alt={s.name}
    className="service-image"
    onError={(e) => {
      console.error('Failed to load image:', s.image_url, 'Full URL:', e.currentTarget.src)
    }}
  />
)}
```

**Выделить и объяснить:**
- ✅ `s.image_url` - значение из БД (например, `january.png`)
- ✅ Проверка `startsWith('http')` - если уже полный URL
- ✅ Формирование URL: `http://localhost:3001/images/january.png`
- ✅ `replace(/^(img\/|images\/|deposits\/img\/)/, '')` - удаляет префиксы если есть
- ✅ В браузере это проксируется на `localhost:5173/images/january.png`

**Объяснение:**
- `image_url` в БД содержит только имя файла (например, `january.png`)
- Код формирует полный URL для загрузки через бэкенд
- Бэкенд получает изображение из MinIO и отдает его

---

### 📸 СКРИНШОТ 10: MinIO Console - список изображений

**Что сделать:**

1. Откройте MinIO Console: http://localhost:9001
2. Войдите:
   - Username: `minioadmin`
   - Password: `minioadmin123`
3. В левом меню найдите **Buckets**
4. Откройте bucket `deposits`
5. Перейдите в папку `img/`

**Что показать на скриншоте:**
- ✅ MinIO Console открыт
- ✅ Bucket `deposits` выбран
- ✅ Папка `img/` открыта
- ✅ Список файлов:
  - `january.png`
  - `february.png`
  - `march.png`
  - И другие изображения (если есть)
- ✅ Можно показать превью изображений (если доступно)

**Дополнительно:** Покажите детали файла:
- ✅ Имя файла
- ✅ Размер файла
- ✅ Дата последнего изменения

**Объяснение:**
- Изображения хранятся в MinIO по пути `deposits/img/<имя_файла>.png`
- Бэкенд получает их через MinIO API
- Отдает через endpoint `/images/:name`
- Фронтенд загружает через proxy

---

## 5. ЧАСТЬ 4: ИЗМЕНЕНИЯ В БД

### 📸 СКРИНШОТ 11: Добавление нового месяца в БД

**Что сделать:**

1. Откройте Adminer: http://localhost:8080
2. Войдите:
   - **Система:** PostgreSQL
   - **Сервер:** deposits-db
   - **Пользователь:** user
   - **Пароль:** password
   - **База данных:** deposits
3. Нажмите **SQL команда** (или вкладка SQL)
4. Выполните SQL:

```sql
INSERT INTO services (name, description, price, image_url, is_active) 
VALUES ('Апрель', 'Дней: 30 | Ставка: 6.0% | Сезон: Весна', 6.0, 'april.png', true);
```

5. Нажмите **Выполнить** (или F5)

**Что показать на скриншоте:**
- ✅ Adminer открыт
- ✅ SQL редактор с запросом
- ✅ Результат: "1 row affected" или "INSERT 0 1"
- ✅ Таблица `services` с новой записью:
  - id: (автоматически)
  - name: 'Апрель'
  - description: 'Дней: 30 | Ставка: 6.0% | Сезон: Весна'
  - price: 6.0
  - image_url: 'april.png'
  - is_active: true

**Дополнительно:** Покажите список всех услуг:

```sql
SELECT id, name, price, image_url FROM services WHERE is_active = true;
```

---

### 📸 СКРИНШОТ 12: Новый месяц появился во фронтенде

**Что сделать:**

1. Вернитесь во фронтенд: http://localhost:5173
2. Откройте DevTools (F12) → вкладка **Network**
3. Обновите страницу (F5)
4. Покажите запрос и результат

**Что показать на скриншоте:**

**Браузер:**
- ✅ Страница "Банковские месяцы"
- ✅ В списке появился новый месяц "Апрель"
- ✅ Всего месяцев: 4 (Январь, Февраль, Март, Апрель)

**DevTools → Network:**
- ✅ Запрос: `GET /api/services`
- ✅ Статус: `200 OK`
- ✅ Response содержит новый месяц "Апрель"

**Вкладка Response:**
- ✅ JSON массив с 4 услугами
- ✅ Последняя запись: `{"id": 4, "name": "Апрель", ...}`

**Объяснение:**
- Изменения в БД сразу видны во фронтенде
- Фронтенд делает запрос к бэкенду
- Бэкенд возвращает актуальные данные из PostgreSQL
- Компонент обновляется автоматически

---

### 📸 СКРИНШОТ 13: Изменение данных в БД

**Что сделать:**

1. В Adminer выполните SQL:

```sql
UPDATE services 
SET price = 7.5, 
    description = 'Дней: 31 | Ставка: 7.5% | Сезон: Зима | Праздничные дни: 8' 
WHERE name = 'Январь';
```

2. Нажмите **Выполнить**

3. Вернитесь во фронтенд и обновите страницу (F5)

**Что показать на скриншоте:**

**Adminer:**
- ✅ SQL запрос в редакторе
- ✅ Результат: "1 row affected" или "UPDATE 1"
- ✅ Таблица `services`: запись "Январь" с price = 7.5

**Фронтенд:**
- ✅ Страница "Банковские месяцы"
- ✅ Карточка "Январь" с обновленной ценой: **7.5%** (было 5.5%)
- ✅ Обновленное описание

**Network:**
- ✅ Новый запрос `GET /api/services`
- ✅ Response с обновленными данными для "Январь"

**Объяснение:**
- Изменения в БД отражаются во фронтенде после обновления
- Фронтенд всегда получает актуальные данные с бэкенда
- Нет кэширования на клиенте (данные всегда свежие)

---

## 6. ЧАСТЬ 5: ОБЪЯСНЕНИЕ КОДА

### 📸 СКРИНШОТ 14: ServicesPage - структура и импорты

**Что сделать:**

1. Откройте файл `src/pages/ServicesPage.tsx`
2. Покажите начало файла (строки 1-16)

**Что показать на скриншоте:**

```tsx
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { addServiceToDraft, getDraftRequest, getServices } from '../api/http'
import type { Service } from '../api/types'
import { useAuth, useRole } from '../context/AuthContext'

export function ServicesPage() {
  const [services, setServices] = useState<Service[]>([])
  const [query, setQuery] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [info, setInfo] = useState<string | null>(null)
  const [cartCount, setCartCount] = useState(0)
  const role = useRole()
  const { user } = useAuth()
```

**Выделить и объяснить:**
- ✅ **Импорты:**
  - `useState`, `useEffect` - React хуки
  - `Link` - навигация из react-router-dom
  - `getServices`, `addServiceToDraft` - API функции
  - `Service` - TypeScript тип
  - `useAuth`, `useRole` - Context хуки
- ✅ **Хуки состояния:**
  - `services` - список услуг (массив)
  - `query` - поисковый запрос (строка)
  - `loading` - загрузка (boolean)
  - `error` - ошибки (string | null)
  - `info` - информационные сообщения (string | null)
  - `cartCount` - количество в корзине (number)
- ✅ **Context хуки:**
  - `role` - роль пользователя (user, moderator, guest)
  - `user` - данные пользователя (email, id, name)

---

### 📸 СКРИНШОТ 15: ServicesPage - функция load()

**Что сделать:**

1. Покажите функцию `load()` (строки 17-28)

**Что показать на скриншоте:**

```tsx
async function load() {
  setLoading(true)
  setError(null)
  try {
    const data = await getServices(query)
    setServices(data)
  } catch (err) {
    setError(err instanceof Error ? err.message : 'Ошибка загрузки месяцев')
  } finally {
    setLoading(false)
  }
}
```

**Выделить и объяснить:**
- ✅ **`async function`** - асинхронная функция (работает с Promise)
- ✅ **`setLoading(true)`** - начало загрузки (показываем индикатор)
- ✅ **`setError(null)`** - очистка предыдущих ошибок
- ✅ **`try/catch/finally`** - обработка ошибок
- ✅ **`await getServices(query)`** - вызов API с параметром поиска
- ✅ **`setServices(data)`** - обновление состояния (список услуг)
- ✅ **`setLoading(false)`** - конец загрузки (скрываем индикатор)

**Объяснение:**
- Функция загружает данные с сервера
- Передает `query` для фильтрации
- Обновляет состояние компонента
- Обрабатывает ошибки

---

### 📸 СКРИНШОТ 16: ServicesPage - useEffect

**Что сделать:**

1. Покажите `useEffect` (строки 44-48)

**Что показать на скриншоте:**

```tsx
useEffect(() => {
  void load()
  void loadCartCount()
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [])
```

**Выделить и объяснить:**
- ✅ **`useEffect`** - хук для побочных эффектов
- ✅ **Пустой массив `[]`** - выполняется только один раз при монтировании
- ✅ **`void load()`** - вызов функции загрузки (игнорируем Promise)
- ✅ **`void loadCartCount()`** - загрузка количества в корзине
- ✅ **Комментарий eslint** - отключает предупреждение о зависимостях

**Объяснение:**
- Выполняется после монтирования компонента
- Загружает начальные данные
- Не зависит от изменений состояния (пустой массив зависимостей)

---

### 📸 СКРИНШОТ 17: ServicesPage - handleSearch

**Что сделать:**

1. Покажите функцию `handleSearch` (строки 50-53)
2. Покажите форму с этой функцией (строки 76-89)

**Что показать на скриншоте:**

```tsx
async function handleSearch(e: React.FormEvent) {
  e.preventDefault()
  await load()
}

// В JSX:
<form onSubmit={handleSearch} className="toolbar">
  <input
    type="search"
    placeholder="Поиск по названию месяца..."
    value={query}
    onChange={(e) => setQuery(e.target.value)}
  />
  <button type="submit" disabled={loading}>
    Найти
  </button>
</form>
```

**Выделить и объяснить:**
- ✅ **`handleSearch`** - обработчик submit формы
- ✅ **`e.preventDefault()`** - предотвращает перезагрузку страницы
- ✅ **`await load()`** - вызывает загрузку с текущим `query`
- ✅ **`onSubmit={handleSearch}`** - привязка к форме
- ✅ **`value={query}`** - контролируемый input (значение из состояния)
- ✅ **`onChange={(e) => setQuery(e.target.value)}`** - обновление состояния при вводе

**Объяснение:**
- Пользователь вводит текст → обновляется `query`
- Пользователь нажимает "Найти" → вызывается `handleSearch`
- `handleSearch` вызывает `load()` → загрузка с фильтром

---

### 📸 СКРИНШОТ 18: http.ts - функция getServices

**Что сделать:**

1. Откройте файл `src/api/http.ts`
2. Покажите функцию `getServices` (строки 85-97)

**Что показать на скриншоте:**

```tsx
export function getServices(query?: string): Promise<Service[]> {
  if (USE_MOCK) {
    // Имитация асинхронного запроса с задержкой
    return new Promise((resolve) => {
      setTimeout(() => {
        resolve(filterMockServices(query || ''))
      }, 300)
    })
  }
  const q = query ? `?query=${encodeURIComponent(query)}` : ''
  // GET /services допускает гостей, поэтому auth не обязателен
  return apiFetch<Service[]>(`/api/services${q}`, {}, { auth: !!getToken() })
}
```

**Выделить и объяснить:**
- ✅ **`USE_MOCK`** - флаг для переключения режимов
- ✅ **Mock режим:**
  - Возвращает Promise с задержкой 300ms
  - Вызывает `filterMockServices(query)` - фильтрация на клиенте
- ✅ **Реальный режим:**
  - Формирует query string: `?query=Январь`
  - `encodeURIComponent` - кодирует спецсимволы
  - Вызывает `apiFetch` с URL `/api/services?query=...`
  - `auth: !!getToken()` - авторизация опциональна

**Объяснение:**
- Одна функция работает в двух режимах
- Mock - для демонстрации без бэкенда
- Реальный - для работы с API

---

### 📸 СКРИНШОТ 19: http.ts - функция apiFetch

**Что сделать:**

1. Покажите функцию `apiFetch` (строки 22-65)

**Что показать на скриншоте:**

```tsx
async function apiFetch<T>(
  path: string,
  options: RequestInit = {},
  opts?: { auth?: boolean },
): Promise<T> {
  const headers: HeadersInit = {
    ...(options.headers || {}),
  }

  if (options.body && !(options.body instanceof FormData)) {
    Object.assign(headers, JSON_HEADERS)
  }

  const needAuth = opts?.auth ?? false
  const token = getToken()

  if (needAuth && token) {
    ;(headers as Record<string, string>).Authorization = `Bearer ${token}`
  }

  const response = await fetch(path, {
    ...options,
    headers,
  })

  if (!response.ok) {
    const text = await response.text()
    let message = `HTTP ${response.status}`
    try {
      const parsed = JSON.parse(text) as { error?: string }
      if (parsed.error) message = parsed.error
    } catch {
      // ignore
    }
    throw new Error(message)
  }

  if (response.status === 204) {
    return null as T
  }

  return (await response.json()) as T
}
```

**Выделить и объяснить:**
- ✅ **Generic `<T>`** - типизированный ответ (TypeScript)
- ✅ **Параметры:**
  - `path` - путь запроса (например, `/api/services`)
  - `options` - опции fetch (method, body, headers)
  - `opts.auth` - нужна ли авторизация
- ✅ **Формирование headers:**
  - JSON headers для body (кроме FormData)
  - Authorization header с JWT токеном (если нужен)
- ✅ **Вызов fetch:**
  - Нативный `fetch` API
  - Путь проксируется через Vite
- ✅ **Обработка ошибок:**
  - Проверка `response.ok`
  - Парсинг ошибки из JSON
  - Выброс исключения

**Объяснение:**
- Универсальная функция для всех API запросов
- Автоматически добавляет авторизацию
- Обрабатывает ошибки единообразно

---

### 📸 СКРИНШОТ 20: vite.config.ts - proxy конфигурация

**Что сделать:**

1. Откройте файл `vite.config.ts`

**Что показать на скриншоте:**

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Проксируем backend, чтобы избежать CORS-проблем
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        secure: false,
      },
      '/images': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        secure: false,
        ws: false,
      },
    },
  },
})
```

**Выделить и объяснить:**
- ✅ **`port: 5173`** - порт Vite dev server
- ✅ **Proxy для `/api`:**
  - Запросы к `localhost:5173/api/*` → `localhost:3001/api/*`
  - `changeOrigin: true` - меняет заголовок Origin
  - `secure: false` - разрешает HTTP
- ✅ **Proxy для `/images`:**
  - Запросы к `localhost:5173/images/*` → `localhost:3001/images/*`
  - `ws: false` - отключает WebSocket проксирование

**Объяснение:**
- Проксирование позволяет избежать CORS проблем
- Все запросы идут через один домен (localhost:5173)
- Прозрачное перенаправление на бэкенд

---

### 📸 СКРИНШОТ 21: DraftPage - расчет прибыли

**Что сделать:**

1. Откройте файл `src/pages/DraftPage.tsx`
2. Покажите функции расчета (строки 36-74)

**Что показать на скриншоте:**

```tsx
// Извлекаем количество дней из description (формат: "Дней: 31 | ...")
function extractDays(description?: string): number {
  if (!description) return 30 // По умолчанию 30 дней
  const match = description.match(/Дней:\s*(\d+)/i)
  return match ? parseInt(match[1], 10) : 30
}

// Рассчитываем прибыль для одного месяца: сумма * (ставка/100) * (дней/365)
function calculateProfit(amount: number, rate: number, days: number): number {
  return amount * (rate / 100) * (days / 365)
}

// Общая сумма вклада (можно сделать настраиваемой, пока используем фиксированную)
const depositAmount = 100000 // 100 000 руб. - как во 2-й лабе

const totals = useMemo(() => {
  if (!draft || draft.services.length === 0) {
    return { totalAmount: 0, totalProfit: 0, averageRate: 0 }
  }

  let totalAmount = 0
  let totalProfit = 0
  let totalQuantity = 0
  let weightedSum = 0

  draft.services.forEach((item) => {
    const days = extractDays(item.service.description)
    const itemAmount = depositAmount * item.quantity
    const profit = calculateProfit(depositAmount, item.service.price, days) * item.quantity

    totalAmount += itemAmount
    totalProfit += profit
    totalQuantity += item.quantity
    weightedSum += item.quantity * item.service.price
  })

  const averageRate = totalQuantity > 0 ? weightedSum / totalQuantity : 0

  return { totalAmount, totalProfit, averageRate }
}, [draft])
```

**Выделить и объяснить:**
- ✅ **`extractDays`** - парсит "Дней: 31" через regex
- ✅ **`calculateProfit`** - формула: `сумма * (ставка/100) * (дней/365)`
- ✅ **`depositAmount = 100000`** - фиксированная сумма вклада
- ✅ **`useMemo`** - мемоизация (пересчитывается только при изменении `draft`)
- ✅ **Итерация `forEach`** - для каждой услуги:
  - Извлекает дни
  - Рассчитывает прибыль с учетом количества
  - Суммирует значения
- ✅ **Средняя ставка** - взвешенная по количеству

**Пример расчета:**
- Январь: 100000 * (5.5/100) * (31/365) = 467.12 руб.
- Количество: 2 → прибыль: 467.12 * 2 = 934.24 руб.

---

### 📸 СКРИНШОТ 22: RequestsPage - фильтрация по ролям

**Что сделать:**

1. Откройте файл `src/pages/RequestsPage.tsx`
2. Покажите использование роли (строки 6-10, 77-94)

**Что показать на скриншоте:**

```tsx
export function RequestsPage() {
  const [requests, setRequests] = useState<Request[]>([])
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const role = useRole()  // Получаем роль из Context

  // В JSX:
  {role === 'moderator' && (
    <>
      <button
        type="button"
        className="link-button"
        onClick={() => handleComplete(r.id)}
      >
        Завершить
      </button>
      <button
        type="button"
        className="link-button"
        onClick={() => handleDelete(r.id)}
      >
        Удалить
      </button>
    </>
  )}
```

**Выделить и объяснить:**
- ✅ **`useRole()`** - получает роль из AuthContext
- ✅ **Условный рендеринг:** `{role === 'moderator' && ...}`
- ✅ **Кнопки видны только модератору**
- ✅ **На бэкенде:** фильтрация тоже по роли:
  - User видит только свои заявки
  - Moderator видит все заявки

**Объяснение:**
- Роль хранится в AuthContext
- Компонент получает роль через хук
- Условно рендерит элементы в зависимости от роли

---

### 📸 СКРИНШОТ 23: AuthContext - управление состоянием

**Что сделать:**

1. Откройте файл `src/context/AuthContext.tsx`

**Что показать на скриншоте:**
- ✅ Структуру Context
- ✅ Функции `login`, `logout`
- ✅ Хуки `useAuth`, `useRole`
- ✅ Сохранение токена в localStorage

**Объяснение:**
- Context предоставляет глобальное состояние авторизации
- Токен сохраняется в localStorage
- Все компоненты могут получить данные пользователя

---

### 📸 СКРИНШОТ 24: ProtectedRoute - защита роутов

**Что сделать:**

1. Откройте файл `src/components/ProtectedRoute.tsx`

**Что показать на скриншоте:**
- ✅ Проверку авторизации
- ✅ Редирект на `/login` для неавторизованных
- ✅ Рендеринг дочерних компонентов для авторизованных

**Объяснение:**
- Компонент защищает роуты от неавторизованного доступа
- Проверяет наличие токена
- Редиректит на страницу входа если нет токена

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ МОМЕНТЫ

### 🔍 Проверка работы proxy

**В Network покажите:**
- Request URL: `http://localhost:5173/api/services`
- В Headers → General → Remote Address может быть `127.0.0.1:5173`
- Но фактически запрос идет на `localhost:3001`

### 🔍 Проверка работы MinIO

**В Network покажите запрос изображения:**
- Request URL: `http://localhost:5173/images/january.png`
- Response Headers: `content-type: image/png`
- Preview: само изображение

### 🔍 Проверка изменений в БД

**Покажите цепочку:**
1. SQL запрос в Adminer
2. Результат в БД
3. Network запрос во фронтенде
4. Обновленные данные на странице

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Перед демонстрацией убедитесь:

- ✅ Mock режим работает (без бэкенда)
- ✅ Реальный режим работает (с бэкендом)
- ✅ Бэкенд запущен и доступен
- ✅ Proxy работает корректно
- ✅ Изображения загружаются из MinIO
- ✅ Изменения в БД отражаются во фронтенде
- ✅ Все компоненты работают
- ✅ Авторизация работает
- ✅ Фильтрация работает

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

### Запуск в mock режиме:
```cmd
cd C:\spa-frontend\spa-deposits
ЗАПУСТИТЬ_MOCK.bat
```

### Запуск с бэкендом:
```cmd
cd C:\spa-frontend\spa-deposits
ЗАПУСТИТЬ_С_БЭКЕНДОМ.bat
```

### Остановка бэкенда:
```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose stop deposits-app
```

### Запуск бэкенда:
```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose up -d deposits-app
```

---

**Удачи на демонстрации! 🚀**


