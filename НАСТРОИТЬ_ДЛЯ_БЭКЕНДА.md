# 🔧 НАСТРОЙКА ДЛЯ РАБОТЫ С БЭКЕНДОМ

## ✅ ЧТО НУЖНО СДЕЛАТЬ:

### Шаг 1: Выключить mock режим

**Способ 1: Удалить .env**
```cmd
cd C:\spa-frontend\spa-deposits
del .env
```

**Способ 2: Изменить .env**
```cmd
cd C:\spa-frontend\spa-deposits
echo VITE_USE_MOCK=false > .env
```

---

### Шаг 2: Обновить ставки в БД

**Вариант 1: Через Adminer (РЕКОМЕНДУЕТСЯ)**

1. Откройте Adminer: http://localhost:8080
2. Войдите: user / password / deposits
3. Выполните SQL из файла `SPA_Dep/ОБНОВИТЬ_СТАВКИ_В_БД.sql`:

```sql
UPDATE services 
SET price = 10.50,
    description = 'Дней: 31 | Ставка: 10.5% | Сезон: Зима | Праздничные дни: 8'
WHERE name = 'Январь';

UPDATE services 
SET price = 10.20,
    description = 'Дней: 28 | Ставка: 10.2% | Сезон: Зима | Праздничные дни: 1'
WHERE name = 'Февраль';

UPDATE services 
SET price = 10.00,
    description = 'Дней: 31 | Ставка: 10.0% | Сезон: Весна | Праздничные дни: 1'
WHERE name = 'Март';
```

**Вариант 2: Через docker exec**

```cmd
cd C:\spa-frontend\SPA_Dep
docker exec -i deposits-db psql -U user -d deposits -c "UPDATE services SET price = 10.50, description = 'Дней: 31 | Ставка: 10.5% | Сезон: Зима | Праздничные дни: 8' WHERE name = 'Январь';"
docker exec -i deposits-db psql -U user -d deposits -c "UPDATE services SET price = 10.20, description = 'Дней: 28 | Ставка: 10.2% | Сезон: Зима | Праздничные дни: 1' WHERE name = 'Февраль';"
docker exec -i deposits-db psql -U user -d deposits -c "UPDATE services SET price = 10.00, description = 'Дней: 31 | Ставка: 10.0% | Сезон: Весна | Праздничные дни: 1' WHERE name = 'Март';"
```

---

### Шаг 3: Запустить бэкенд

```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose up -d deposits-app
```

Подождите 15 секунд для инициализации.

---

### Шаг 4: Перезапустить фронтенд

```cmd
cd C:\spa-frontend\spa-deposits
taskkill /F /IM node.exe
npm run dev
```

---

### Шаг 5: Проверить работу

1. Откройте: http://localhost:5173
2. Нажмите **Ctrl+Shift+R** (жесткая перезагрузка)
3. Должны быть видны:
   - ✅ Ставки: 10.5%, 10.2%, 10.0%
   - ✅ Изображения из MinIO (если они есть в MinIO)

---

## 🚀 БЫСТРЫЙ СПОСОБ:

Используйте скрипт:

```cmd
cd C:\spa-frontend\spa-deposits
НАСТРОИТЬ_ДЛЯ_БЭКЕНДА.bat
```

Этот скрипт:
1. Удалит .env (выключит mock)
2. Проверит и запустит бэкенд
3. Перезапустит фронтенд

**После этого обновите ставки в БД через Adminer!**

---

## 📝 ПРОВЕРКА СТАВОК В БД:

Выполните в Adminer:

```sql
SELECT id, name, price, description FROM services WHERE name IN ('Январь', 'Февраль', 'Март');
```

**Должно быть:**
- Январь: price = 10.50
- Февраль: price = 10.20
- Март: price = 10.00

---

**Готово! 🎉**


