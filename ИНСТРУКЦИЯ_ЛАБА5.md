# 🔬 ЛАБОРАТОРНАЯ РАБОТА 5 (spa-deposits) - ПОЛНАЯ ИНСТРУКЦИЯ

## 📋 СОДЕРЖАНИЕ

1. [Подготовка](#1-подготовка)
2. [Часть 1: Mock режим (без бэкенда)](#2-часть-1-mock-режим-без-бэкенда)
3. [Часть 2: С бэкендом](#3-часть-2-с-бэкендом)
4. [Часть 3: Network и MinIO](#4-часть-3-network-и-minio)
5. [Часть 4: Изменения в БД](#5-часть-4-изменения-в-бд)
6. [Часть 5: Объяснение кода](#6-часть-5-объяснение-кода)

---

## 1. ПОДГОТОВКА

### 📍 Шаг 1: Остановить бэкенд (для демонстрации mock)

```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose stop deposits-app
```

**Или оставить запущенным** - mock режим работает независимо.

---

## 2. ЧАСТЬ 1: MOCK РЕЖИМ (БЕЗ БЭКЕНДА)

### 📍 Шаг 1: Включить mock режим

1. Перейдите в папку проекта:
   ```cmd
   cd C:\spa-frontend\spa-deposits
   ```

2. Создайте файл `.env` в корне проекта:
   ```
   VITE_USE_MOCK=true
   ```

3. Запустите dev server:
   ```cmd
   npm run dev
   ```

4. Откройте в браузере: http://localhost:5173

---

### 📸 СКРИНШОТ 1: Главная страница (ServicesPage) с mock данными

**Действия:**

1. Откройте: http://localhost:5173
2. Должна открыться страница "Банковские месяцы"
3. Покажите список месяцев (Январь, Февраль, Март)

**Скриншот должен показать:**
- ✅ Страница "Банковские месяцы"
- ✅ Список месяцев из mock данных
- ✅ Поле поиска
- ✅ Кнопка "Добавить в расчет" (если авторизованы)

**Важно:** В консоли браузера (F12) не должно быть ошибок о недоступности API.

---

### 📸 СКРИНШОТ 2: Работа поиска с mock данными

**Действия:**

1. В поле поиска введите: `Январь`
2. Нажмите "Найти"
3. Должен остаться только "Январь"

**Скриншот должен показать:**
- ✅ Поле поиска с текстом "Январь"
- ✅ Отфильтрованный список (только "Январь")
- ✅ Объяснить, что фильтрация работает на клиенте (в `filterMockServices`)

**Код фильтрации (mock.ts):**
```typescript
export function filterMockServices(query: string): Service[] {
  if (!query) return mockServices
  const lowerQuery = query.toLowerCase()
  return mockServices.filter(
    (service) =>
      service.name.toLowerCase().includes(lowerQuery) ||
      service.description?.toLowerCase().includes(lowerQuery),
  )
}
```

---

### 📸 СКРИНШОТ 3: Страница черновика (DraftPage) с mock данными

**Действия:**

1. Войдите в систему (если еще не вошли):
   - Перейдите на `/login`
   - Войдите с данными: `user@example.com` / `password123`
2. Перейдите на `/draft`
3. Покажите страницу расчета вклада

**Скриншот должен показать:**
- ✅ Страница "Расчет вклада"
- ✅ Таблица с месяцами из mock данных
- ✅ Расчет прибыли
- ✅ Общая сумма и прибыль

**Важно:** Данные берутся из `mockDraft` в `mock.ts`.

---

### 📸 СКРИНШОТ 4: Страница заявок (RequestsPage) с mock данными

**Действия:**

1. Перейдите на `/requests`
2. Покажите список заявок

**Скриншот должен показать:**
- ✅ Страница "Расчеты вкладов"
- ✅ Таблица с заявками из mock данных
- ✅ Статусы заявок (submitted, completed)
- ✅ Кнопки "Завершить" и "Удалить" (для модератора)

**Важно:** Данные берутся из `mockRequests` в `mock.ts`.

---

## 3. ЧАСТЬ 2: С БЭКЕНДОМ

### 📍 Шаг 1: Выключить mock режим

1. В файле `.env` измените:
   ```
   VITE_USE_MOCK=false
   ```

2. Или удалите файл `.env` полностью

3. Перезапустите dev server:
   ```cmd
   # Остановите текущий (Ctrl+C)
   npm run dev
   ```

---

### 📍 Шаг 2: Запустить бэкенд

```cmd
cd C:\spa-frontend\SPA_Dep
docker-compose up -d deposits-app
```

Подождите 10-15 секунд для запуска.

---

### 📸 СКРИНШОТ 5: Главная страница с бэкендом

**Действия:**

1. Откройте: http://localhost:5173
2. Откройте DevTools (F12) → вкладка **Network**
3. Обновите страницу (F5)
4. Покажите запрос к API

**Скриншот должен показать:**
- ✅ Страница "Банковские месяцы"
- ✅ DevTools открыт, вкладка Network
- ✅ Запрос: `GET http://localhost:5173/api/services`
- ✅ В Headers показать, что запрос проксируется на `http://localhost:3001/api/services`
- ✅ Response с данными из БД

**Важно:** Покажите, что данные теперь приходят с бэкенда, а не из mock.

---

### 📸 СКРИНШОТ 6: Работа поиска с бэкендом

**Действия:**

1. В поле поиска введите: `Январь`
2. Нажмите "Найти"
3. В Network покажите новый запрос

**Скриншот должен показать:**
- ✅ Поле поиска с текстом "Январь"
- ✅ Network: `GET http://localhost:5173/api/services?query=Январь`
- ✅ Запрос проксируется на `http://localhost:3001/api/services?query=Январь`
- ✅ Response с отфильтрованными данными

**Важно:** Объяснить, что фильтрация теперь происходит на бэкенде, а не на клиенте.

---

## 4. ЧАСТЬ 3: NETWORK И MINIO

### 📸 СКРИНШОТ 7: Fetch запросы через proxy

**Действия:**

1. Откройте DevTools (F12) → вкладка **Network**
2. Обновите страницу со списком услуг
3. Найдите запрос `GET /api/services`

**Скриншот должен показать:**

**Вкладка Headers:**
- ✅ **Request URL:** `http://localhost:5173/api/services`
- ✅ **General:**
  - Request Method: `GET`
  - Status Code: `200 OK`
- ✅ **Response Headers:**
  - `content-type: application/json`
  - Данные из бэкенда

**Объяснение:**
- Запрос идет на `localhost:5173` (Vite dev server)
- Vite proxy перенаправляет на `localhost:3001` (бэкенд)
- Бэкенд возвращает данные из БД

---

### 📸 СКРИНШОТ 8: Запросы к изображениям через proxy

**Действия:**

1. В Network найдите запросы к изображениям
2. Найдите запрос типа: `GET /images/january.png`

**Скриншот должен показать:**

**Вкладка Headers:**
- ✅ **Request URL:** `http://localhost:5173/images/january.png`
- ✅ **General:**
  - Request Method: `GET`
  - Status Code: `200 OK`
  - Type: `png` или `image/png`
- ✅ **Response Headers:**
  - `content-type: image/png`
  - `content-length: <размер>`

**Вкладка Preview:**
- ✅ Показать само изображение

**Объяснение:**
- Запрос идет на `localhost:5173/images/january.png`
- Vite proxy перенаправляет на `localhost:3001/images/january.png`
- Бэкенд получает изображение из MinIO по пути `img/january.png`
- MinIO возвращает файл через бэкенд

---

### 📸 СКРИНШОТ 9: Код формирования URL изображений

**Действия:**

1. Откройте файл `src/pages/ServicesPage.tsx`
2. Найдите строки 108-111

**Скриншот должен показать:**

```tsx
<img
  src={s.image_url.startsWith('http') 
    ? s.image_url 
    : `http://localhost:3001/images/${s.image_url.replace(/^(img\/|images\/|deposits\/img\/)/, '')}`}
  alt={s.name}
  className="service-image"
/>
```

**Объяснение:**
- `s.image_url` из БД содержит имя файла (например, `january.png`)
- Код проверяет, начинается ли URL с `http` (полный URL)
- Если нет, формирует URL: `http://localhost:3001/images/january.png`
- Удаляет префиксы `img/`, `images/`, `deposits/img/` если есть
- В браузере это проксируется на `localhost:5173/images/january.png`

---

### 📸 СКРИНШОТ 10: MinIO Console - список изображений

**Действия:**

1. Откройте MinIO Console: http://localhost:9001
2. Войдите: `minioadmin` / `minioadmin123`
3. Перейдите в bucket `deposits`
4. Покажите папку `img/` с изображениями

**Скриншот должен показать:**
- ✅ MinIO Console открыт
- ✅ Bucket `deposits` выбран
- ✅ Папка `img/` с файлами:
  - `january.png`
  - `february.png`
  - `march.png`
- ✅ Можно показать превью изображений

**Объяснение:**
- Изображения хранятся в MinIO по пути `img/<имя_файла>.png`
- Бэкенд получает их через MinIO API
- Отдает через endpoint `/images/:name`

---

## 5. ЧАСТЬ 4: ИЗМЕНЕНИЯ В БД

### 📸 СКРИНШОТ 11: Добавление нового месяца в БД

**Действия:**

1. Откройте Adminer: http://localhost:8080
2. Войдите: user / password / deposits
3. Выполните SQL:

```sql
INSERT INTO services (name, description, price, image_url, is_active) 
VALUES ('Апрель', 'Дней: 30 | Ставка: 6.0% | Сезон: Весна', 6.0, 'april.png', true);
```

4. Покажите результат в Adminer

**Скриншот должен показать:**
- ✅ Adminer открыт
- ✅ SQL запрос выполнен
- ✅ Новая запись в таблице `services`
- ✅ Поля: name='Апрель', price=6.0, image_url='april.png'

---

### 📸 СКРИНШОТ 12: Новый месяц появился во фронтенде

**Действия:**

1. Вернитесь во фронтенд: http://localhost:5173
2. Обновите страницу (F5)
3. Откройте DevTools → Network
4. Покажите запрос к API

**Скриншот должен показать:**
- ✅ Страница "Банковские месяцы"
- ✅ В списке появился "Апрель"
- ✅ DevTools → Network: запрос `GET /api/services`
- ✅ Response содержит новый месяц "Апрель"

**Объяснение:**
- Изменения в БД сразу видны во фронтенде
- Фронтенд делает запрос к бэкенду
- Бэкенд возвращает актуальные данные из БД

---

### 📸 СКРИНШОТ 13: Изменение данных в БД

**Действия:**

1. В Adminer измените цену существующего месяца:

```sql
UPDATE services 
SET price = 7.5, description = 'Дней: 31 | Ставка: 7.5% | Сезон: Зима | Праздничные дни: 8' 
WHERE name = 'Январь';
```

2. Обновите страницу во фронтенде

**Скриншот должен показать:**
- ✅ Adminer: SQL запрос выполнен
- ✅ Фронтенд: цена "Январь" изменилась на 7.5%
- ✅ Network: новый запрос к API с обновленными данными

---

## 6. ЧАСТЬ 5: ОБЪЯСНЕНИЕ КОДА

### 📸 СКРИНШОТ 14: Компонент ServicesPage - структура

**Действия:**

1. Откройте файл `src/pages/ServicesPage.tsx`
2. Покажите начало файла (импорты, хуки)

**Скриншот должен показать:**

```tsx
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { addServiceToDraft, getDraftRequest, getServices } from '../api/http'
import type { Service } from '../api/types'
import { useAuth, useRole } from '../context/AuthContext'

export function ServicesPage() {
  const [services, setServices] = useState<Service[]>([])
  const [query, setQuery] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [info, setInfo] = useState<string | null>(null)
  const [cartCount, setCartCount] = useState(0)
  const role = useRole()
  const { user } = useAuth()
```

**Объяснение:**
- **Импорты:** React хуки, API функции, типы, Context
- **Хуки состояния:**
  - `services` - список услуг
  - `query` - поисковый запрос
  - `loading` - состояние загрузки
  - `error` - ошибки
  - `info` - информационные сообщения
  - `cartCount` - количество в корзине
- **Context хуки:**
  - `useRole()` - роль пользователя (user, moderator, guest)
  - `useAuth()` - данные пользователя

---

### 📸 СКРИНШОТ 15: Функция load() - загрузка данных

**Действия:**

1. Покажите функцию `load()` в `ServicesPage.tsx`

**Скриншот должен показать:**

```tsx
async function load() {
  setLoading(true)
  setError(null)
  try {
    const data = await getServices(query)
    setServices(data)
  } catch (err) {
    setError(err instanceof Error ? err.message : 'Ошибка загрузки месяцев')
  } finally {
    setLoading(false)
  }
}
```

**Объяснение:**
- **Асинхронная функция** - использует `async/await`
- **Управление состоянием:**
  - `setLoading(true)` - начало загрузки
  - `setError(null)` - очистка ошибок
  - `setServices(data)` - обновление списка услуг
- **Обработка ошибок:** try/catch/finally
- **Вызов API:** `getServices(query)` - передает поисковый запрос

---

### 📸 СКРИНШОТ 16: useEffect - жизненный цикл

**Действия:**

1. Покажите `useEffect` в `ServicesPage.tsx`

**Скриншот должен показать:**

```tsx
useEffect(() => {
  void load()
  void loadCartCount()
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [])
```

**Объяснение:**
- **useEffect** - выполняется после монтирования компонента
- **Пустой массив зависимостей `[]`** - выполняется только один раз
- **Вызовы функций:**
  - `load()` - загружает список услуг
  - `loadCartCount()` - загружает количество в корзине
- **`void`** - игнорирует Promise (не ждем результат)

---

### 📸 СКРИНШОТ 17: Функция handleSearch - фильтрация

**Действия:**

1. Покажите функцию `handleSearch` в `ServicesPage.tsx`

**Скриншот должен показать:**

```tsx
async function handleSearch(e: React.FormEvent) {
  e.preventDefault()
  await load()
}
```

**Объяснение:**
- **Обработчик формы** - вызывается при submit
- **`e.preventDefault()`** - предотвращает перезагрузку страницы
- **`await load()`** - вызывает загрузку с текущим `query`
- **Связь с формой:**
  ```tsx
  <form onSubmit={handleSearch}>
    <input
      value={query}
      onChange={(e) => setQuery(e.target.value)}
    />
  </form>
  ```

---

### 📸 СКРИНШОТ 18: API функция getServices - вызов fetch

**Действия:**

1. Откройте файл `src/api/http.ts`
2. Покажите функцию `getServices`

**Скриншот должен показать:**

```tsx
export function getServices(query?: string): Promise<Service[]> {
  if (USE_MOCK) {
    // Имитация асинхронного запроса с задержкой
    return new Promise((resolve) => {
      setTimeout(() => {
        resolve(filterMockServices(query || ''))
      }, 300)
    })
  }
  const q = query ? `?query=${encodeURIComponent(query)}` : ''
  // GET /services допускает гостей, поэтому auth не обязателен
  return apiFetch<Service[]>(`/api/services${q}`, {}, { auth: !!getToken() })
}
```

**Объяснение:**
- **Условная логика:** `USE_MOCK` - переключение между mock и API
- **Mock режим:** возвращает Promise с задержкой 300ms
- **Реальный API:**
  - Формирует URL: `/api/services?query=...`
  - Вызывает `apiFetch` с параметрами
  - `auth: !!getToken()` - авторизация опциональна

---

### 📸 СКРИНШОТ 19: Функция apiFetch - работа с fetch

**Действия:**

1. Покажите функцию `apiFetch` в `src/api/http.ts`

**Скриншот должен показать:**

```tsx
async function apiFetch<T>(
  path: string,
  options: RequestInit = {},
  opts?: { auth?: boolean },
): Promise<T> {
  const headers: HeadersInit = {
    ...(options.headers || {}),
  }

  if (options.body && !(options.body instanceof FormData)) {
    Object.assign(headers, JSON_HEADERS)
  }

  const needAuth = opts?.auth ?? false
  const token = getToken()

  if (needAuth && token) {
    ;(headers as Record<string, string>).Authorization = `Bearer ${token}`
  }

  const response = await fetch(path, {
    ...options,
    headers,
  })

  if (!response.ok) {
    const text = await response.text()
    let message = `HTTP ${response.status}`
    try {
      const parsed = JSON.parse(text) as { error?: string }
      if (parsed.error) message = parsed.error
    } catch {
      // ignore
    }
    throw new Error(message)
  }

  return (await response.json()) as T
}
```

**Объяснение:**
- **Generic функция** `<T>` - типизированный ответ
- **Формирование headers:**
  - JSON headers для body
  - Authorization header с JWT токеном (если нужен)
- **Вызов fetch:**
  - Использует нативный `fetch` API
  - Путь проксируется через Vite
- **Обработка ошибок:**
  - Проверка `response.ok`
  - Парсинг ошибки из JSON
  - Выброс исключения

---

### 📸 СКРИНШОТ 20: Vite proxy конфигурация

**Действия:**

1. Откройте файл `vite.config.ts`

**Скриншот должен показать:**

```typescript
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Проксируем backend, чтобы избежать CORS-проблем
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        secure: false,
      },
      '/images': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        secure: false,
        ws: false,
      },
    },
  },
})
```

**Объяснение:**
- **Proxy для `/api`:** все запросы к `/api/*` перенаправляются на `http://localhost:3001/api/*`
- **Proxy для `/images`:** все запросы к `/images/*` перенаправляются на `http://localhost:3001/images/*`
- **`changeOrigin: true`** - меняет заголовок Origin
- **`secure: false`** - разрешает HTTP (не только HTTPS)
- **Преимущества:**
  - Избегает CORS проблем
  - Единый домен для фронтенда и API
  - Прозрачное проксирование

---

### 📸 СКРИНШОТ 21: Компонент DraftPage - расчет прибыли

**Действия:**

1. Откройте файл `src/pages/DraftPage.tsx`
2. Покажите функции расчета

**Скриншот должен показать:**

```tsx
// Извлекаем количество дней из description (формат: "Дней: 31 | ...")
function extractDays(description?: string): number {
  if (!description) return 30 // По умолчанию 30 дней
  const match = description.match(/Дней:\s*(\d+)/i)
  return match ? parseInt(match[1], 10) : 30
}

// Рассчитываем прибыль для одного месяца: сумма * (ставка/100) * (дней/365)
function calculateProfit(amount: number, rate: number, days: number): number {
  return amount * (rate / 100) * (days / 365)
}

// useMemo для расчета итогов
const totals = useMemo(() => {
  if (!draft || draft.services.length === 0) {
    return { totalAmount: 0, totalProfit: 0, averageRate: 0 }
  }

  let totalAmount = 0
  let totalProfit = 0
  let totalQuantity = 0
  let weightedSum = 0

  draft.services.forEach((item) => {
    const days = extractDays(item.service.description)
    const itemAmount = depositAmount * item.quantity
    const profit = calculateProfit(depositAmount, item.service.price, days) * item.quantity

    totalAmount += itemAmount
    totalProfit += profit
    totalQuantity += item.quantity
    weightedSum += item.quantity * item.service.price
  })

  const averageRate = totalQuantity > 0 ? weightedSum / totalQuantity : 0

  return { totalAmount, totalProfit, averageRate }
}, [draft])
```

**Объяснение:**
- **`extractDays`** - парсит количество дней из description через regex
- **`calculateProfit`** - формула: `сумма * (ставка/100) * (дней/365)`
- **`useMemo`** - мемоизация расчетов (пересчитывается только при изменении `draft`)
- **Итерация по услугам:**
  - Извлекает дни для каждой услуги
  - Рассчитывает прибыль с учетом количества
  - Суммирует общие значения
- **Средняя ставка:** взвешенная по количеству

---

### 📸 СКРИНШОТ 22: Компонент RequestsPage - фильтрация по ролям

**Действия:**

1. Откройте файл `src/pages/RequestsPage.tsx`
2. Покажите использование роли

**Скриншот должен показать:**

```tsx
export function RequestsPage() {
  const [requests, setRequests] = useState<Request[]>([])
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const role = useRole()

  async function load() {
    setLoading(true)
    setError(null)
    try {
      const data = await listRequests()
      setRequests(data)
    } catch (err) {
      setError(
        err instanceof Error ? err.message : 'Не удалось загрузить расчеты',
      )
    } finally {
      setLoading(false)
    }
  }

  // В JSX:
  {role === 'moderator' && (
    <>
      <button onClick={() => handleComplete(r.id)}>Завершить</button>
      <button onClick={() => handleDelete(r.id)}>Удалить</button>
    </>
  )}
```

**Объяснение:**
- **`useRole()`** - получает роль из AuthContext
- **Условный рендеринг:** кнопки видны только модератору
- **API запрос:** `listRequests()` - бэкенд фильтрует по роли
  - User видит только свои заявки
  - Moderator видит все заявки

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ СКРИНШОТЫ

### 📸 СКРИНШОТ 23: AuthContext - управление состоянием

**Действия:**

1. Откройте файл `src/context/AuthContext.tsx`

**Скриншот должен показать:**
- ✅ Структуру Context
- ✅ Функции `login`, `logout`
- ✅ Хуки `useAuth`, `useRole`
- ✅ Сохранение токена в localStorage

---

### 📸 СКРИНШОТ 24: ProtectedRoute - защита роутов

**Действия:**

1. Откройте файл `src/components/ProtectedRoute.tsx`

**Скриншот должен показать:**
- ✅ Проверку авторизации
- ✅ Редирект на `/login` для неавторизованных
- ✅ Рендеринг дочерних компонентов для авторизованных

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Перед демонстрацией убедитесь:

- ✅ Mock режим работает (без бэкенда)
- ✅ Реальный режим работает (с бэкендом)
- ✅ Proxy настроен правильно
- ✅ Изображения загружаются из MinIO
- ✅ Изменения в БД отражаются во фронтенде
- ✅ Все компоненты работают корректно

---

**Удачи на демонстрации! 🚀**


